org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Backend the app talks to. 10.0.2.2 is the emulator's alias for the host machine.
# Override with -PbonvoyBaseUrl=https://... or BONVOY_BASE_URL env var.
bonvoyBaseUrl=http://10.0.2.2:3000

# Devin org that backend-triggered sessions are created in (sent as devinOrgId).
# Override with -PdevinOrgId=org-... or DEVIN_ORG_ID env var.
devinOrgId=org-f68fd919778f498e8ff07a735d842847
